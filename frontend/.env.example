# Endpoint único de la aplicación. No existe ninguna ruta REST.
VITE_GRAPHQL_HTTP=http://localhost:4000/graphql

# Mismo endpoint sobre WebSocket, para las Subscriptions.
VITE_GRAPHQL_WS=ws://localhost:4000/graphql
